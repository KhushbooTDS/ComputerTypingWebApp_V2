@model List<Handicap>

@{
    Layout = "~/Views/Shared/_SuperAdminLayout.cshtml";
}
<input class="btn-info" type="button" value="Add Handicap" onclick="location.href='@Url.Action("AddHandicap","SuperAdmin")'" />
<hr />

<div class="container">
    <div class="card">
        <h5 class="card-header">Handicap Report</h5>
        <div class="card-body">
            <div class="table-responsive text-nowrap">
                <table class="table table-bordered">
                    <tr>
                        <th>
                            Handicap Name
                        </th>
                    </tr>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.HadicapName)
                            </td>
                        </tr>
                    }
                </table>
            </div>
        </div>
    </div>
</div>
